{
  "name": "@icecream/ui-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "dependencies": {
    "wsrun": "^5.2.4",
    "@chainsafe/chainbridge-ui-core": "1.0.3"
  },
  "scripts": {
    "start:swap": "yarn wsrun -p @icecream/swap -c start",
    "start:bridge": "yarn wsrun -p @chainsafe/chainbridge-ui -c start",
    "build:core": "yarn wsrun -p @chainsafe/chainbridge-ui-core -c build"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/webpack-dev-server",
      "**/react-scripts"
    ]
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
